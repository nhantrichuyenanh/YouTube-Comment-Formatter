TODO:
- [ ] Fix the link regex to properly render all written variations of hyperlink (e.g. `github.com`, `www.github.com`, `https://github.com`, etc...) in preview.
- [ ] Fix preview to not format timestamps, links, and hashtags when they are formatted with formatting symbols.
